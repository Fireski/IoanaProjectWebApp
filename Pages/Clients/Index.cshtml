@page
@model IoanaProjectWebApp.Pages.Clients.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Clienti[0].Nume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clienti[0].Prenume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clienti[0].DataComanda)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clienti[0].Problema)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clienti[0].Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clienti[0].PhoneNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Clienti) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prenume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataComanda)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Problema)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
